require 'spec_helper'

describe Legend::Command do
	context "When given the look command" do
		it 'returns the description' do
			pending "we don't know what to do"
  	  #results = Legend::Command.new.look
      #desc = Legend::Client.new.description
      #results.should == desc
  	end
  end
end